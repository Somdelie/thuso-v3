import { NewJobForm } from "@/components/client/projects/NewJobForm";

export const metadata = {
  title: "Jobs",
};

const CreateProject = () => {
  return <NewJobForm />;
};

export default CreateProject;
